// eslint-disable-next-line check-file/folder-naming-convention
export default function UserDashboardMatchIdLayout( { children }: { children: React.ReactNode } ) {
  return (
    <div>
      {children}
    </div>
  )
}
